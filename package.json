{
  "name": "nework-simulator",
  "version": "1.0.0",
  "description": "to simulate networks",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "live-server src --port=8080"
  },
  "author": "akram",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
